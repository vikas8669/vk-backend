name: Keep Render Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl https://portfolio-crsz.onrender.com/api/health
